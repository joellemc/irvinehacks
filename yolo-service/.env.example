# Optional: used when running the YOLO service (yolo-service/)
# Copy to .env and edit as needed.

# Path to YOLOv8 .pt weights. Default: yolov8n.pt (COCO). Use a food model for milk/egg etc.
# YOLO_MODEL_PATH=./yolov8n.pt
# YOLO_MODEL_PATH=./food_model.pt

# Only applies when using the default COCO model (yolov8n.pt).
# 1 = return only food/container classes (banana, apple, bottle, cup, ...). 0 = return all 80 COCO classes.
# When YOLO_MODEL_PATH is a custom model, filtering is OFF by default so all model classes are returned.
# YOLO_FOOD_ONLY=1

# Use a Roboflow-hosted model by ID (e.g. ingredients-u1v6l-os2k0/1). No local .pt needed.
# ROBOFLOW_MODEL=ingredients-u1v6l-os2k0/1
# ROBOFLOW_API_KEY=your_roboflow_api_key
# Minimum confidence 0–1 for detections (default 0.5 = 50%).
# ROBOFLOW_CONFIDENCE=0.5

# Use SAM 3 for detection: USE_SAM3=1 and ROBOFLOW_API_KEY. Optional: SAM3_PROMPTS=egg,milk,tomato,...
# USE_SAM3=1
# SAM3_PROMPTS=egg,milk,tomato,onion,carrot,broccoli,chicken,cheese,bread
# SAM3_CONFIDENCE=0.4
